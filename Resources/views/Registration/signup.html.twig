{% extends 'EpixaSimpleUserBundle::layout.html.twig' %}

{% block content %}
    <form action="{{ path('signup') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}

        <input type="submit" value="Register" />
    </form>
{% endblock %}